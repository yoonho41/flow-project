version: 1.0
runtime: corretto11

build:
  commands:
    pre-build:
      - chmod +x gradlew
      - ./gradlew --version
    build:
      - ./gradlew clean bootJar -x test

run:
  command: java -jar build/libs/app.jar
  network:
    port: 8080